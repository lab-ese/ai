1. PSEUDO PY CODE:
def solve_n_queens(board, row, N):
    if row >= N:
        return True
    
    for col in range(N):
        if is_safe(board, row, col):
            board[row][col] = 1
            if solve_n_queens(board, row + 1, N):
                return True
            board[row][col] = 0  # Backtrack
            
    return False

2. ALGORITHM IN STEPS:
Step 1: Create an empty NxN grid.
Step 2: Start a recursive function from row 0.
Step 3: If the current row equals N, all queens are placed successfully; return True.
Step 4: For the current row, iterate through each column.
Step 5: Check if placing a queen at (row, col) is safe (no vertical or diagonal conflicts).
Step 6: If safe, place the queen and recursively call the function for row + 1.
Step 7: If the recursive call succeeds, return True.
Step 8: If it fails, backtrack by removing the queen from (row, col) and try the next column.

3. DETAILED DRY RUN:
Problem: 4-Queens
Initial State: 4x4 empty board.
Row 0: Place Q at (0,0). Board: Q _ _ _
Row 1: Check (1,0) -> Unsafe. Check (1,1) -> Unsafe. Place Q at (1,2). Board: _ _ Q _
Row 2: Check all cols (0 to 3) -> All are attacked by Q(0,0) and Q(1,2).
Row 2 Fails. Backtrack to Row 1.
Row 1: Remove Q at (1,2). Place Q at (1,3). Board: _ _ _ Q
Row 2: Place Q at (2,1). Board: _ Q _ _
Row 3: Check all cols -> All are attacked!
Row 3 Fails. Backtrack to Row 2, which has no more cols. Backtrack to Row 1, which has no more cols. Backtrack to Row 0.
Row 0: Move Q from (0,0) to (0,1). Board: _ Q _ _
Row 1: Place Q at (1,3).
Row 2: Place Q at (2,0).
Row 3: Place Q at (3,2).
Success! Final positions: (0,1), (1,3), (2,0), (3,2).
